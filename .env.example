# Telegram bot token from @BotFather (/newbot)
BOT_TOKEN=

# Where to store the SQLite database (default: ./telewordle.db)
# DB_PATH=telewordle.db

# Optional: Claude API key for AI-written word explanations after each game.
# Without it the bot falls back to free dictionary APIs (en + ru).
# ANTHROPIC_API_KEY=sk-ant-...
